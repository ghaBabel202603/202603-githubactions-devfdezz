name: 05 - Event Filters
on:
    workflow_dispatch: 
#   push:
#        branches: 
#            - main
jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: echo
              run: echo "Running whenever branch is main" 